class Keyword(object):
    def __init__(self, coef, count, tag, university):
        self.coef = coef
        self.count = count
        self.tag = tag
        self.university = university

    # def keyword_decoder(self):
    #     return Keyword(self['title'], self['description'], self.['link'], self['pub_date'])
